{
	"name": "talk-template",
	"version": "0.2.0",
	"license": "MIT",
	"private": true,
	"scripts": {
		"clear:cache": "rm -rf .parcel-cache",
		"clear:dist": "rm -rf dist",
		"clear": "pnpm run clear:dist && pnpm run clear:cache",
		"predev": "pnpm run clear",
		"dev": "parcel src/index.html -p 6001",
		"deps": "pnpm up -r --latest",
		"deps:push": "git add . && git commit -m \"build(deps): bumped external dependencies\" && git push",
		"prebuild": "pnpm run clear",
		"build": "parcel build src/index.html --no-optimize --public-url ./",
		"rel:push": "git push && git push --tags",
		"rel:major": "pnpm run build && git add . && pnpm version major --force -m \"rel(%s)\" && pnpm run rel:push",
		"rel:minor": "pnpm run build && git add . && pnpm version minor --force -m \"rel(%s)\" && pnpm run rel:push",
		"rel:patch": "pnpm run build && git add . && pnpm version patch --force -m \"rel(%s)\" && pnpm run rel:push",
		"format:prettier": "prettier --write \"**/*.{html,css,md,json,yml,yaml}\"",
		"format:biome": "biome format --write .",
		"lint:biome": "biome lint .",
		"format": "pnpm run format:prettier && pnpm run format:biome",
		"lint": "pnpm run lint:biome"
	},
	"dependencies": {
		"@pixu-talks/core": "workspace:*",
		"@pixu-talks/fonts": "workspace:*",
		"@pixu-talks/theme": "workspace:*",
		"reveal.js": "6.0.1"
	},
	"devDependencies": {
		"@biomejs/biome": "2.4.13",
		"@parcel/transformer-inline-string": "2.16.4",
		"@parcel/transformer-sass": "2.16.4",
		"baseline-browser-mapping": "2.10.21",
		"lightningcss": "1.32.0",
		"parcel": "2.16.4",
		"posthtml": "0.16.7",
		"posthtml-include": "2.0.1",
		"prettier": "3.8.3",
		"sharp": "0.33.5",
		"svgo": "4.0.1"
	}
}
